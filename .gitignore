# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend (when P2/P3 land)
node_modules/
.next/
.turbo/
dist/
build/
.cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Sim runtime artifacts
backend/tests/last_result.json
# NOTE: backend/cache/ IS committed — canonical RTO v1/v2 results live there
# so demo loads instantly even after Railway restarts (ephemeral disk).
/tmp/uvicorn.log

# Don't ship the MiroFish reference clone
MiroFish/
